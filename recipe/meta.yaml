{% set name = "appium-python-client" %}
{% set version = "2.2.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/Appium-Python-Client-{{ version }}.tar.gz
  sha256: bc823213d7dcce58f03bc16d2db2d77b9cbf772a59d6f8f94cc09a70fac4468e

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python
  run:
    - python
    - selenium >=3.14.1,<4

test:
  imports:
    - appium
    - appium.common
  commands:
    - pip check
  requires:
    - pip

about:
  home: http://appium.io/
  summary: Python client for Appium
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - saibhargav1508
